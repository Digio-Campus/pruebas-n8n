# ======================================
# N8N Environment Configuration Example
# ======================================
# Copia este archivo como .env y configura tus valores reales
# ⚠️  NUNCA commitees el archivo .env real con credenciales

# ==========================================
# 🐳 DOCKER & N8N BASIC CONFIGURATION
# ==========================================

# Puerto donde correrá n8n (por defecto 5678)
N8N_PORT=5678

# Zona horaria
TIMEZONE=Europe/Madrid

# Nivel de logs (error, warn, info, verbose, debug)
N8N_LOG_LEVEL=info

# URL base para webhooks
WEBHOOK_URL=http://localhost:5678

# ==========================================
# 🔐 AUTHENTICATION & SECURITY
# ==========================================

# Activar autenticación básica (true/false)
N8N_BASIC_AUTH_ACTIVE=true

# Usuario para login básico
N8N_BASIC_AUTH_USER=admin

# Contraseña para login básico (CAMBIAR!)
N8N_BASIC_AUTH_PASSWORD=your_secure_password_here

# Clave de encriptación para credenciales (GENERAR UNA NUEVA!)
# Genera una con: openssl rand -base64 32
N8N_ENCRYPTION_KEY=your_encryption_key_here_generate_a_new_one

# ==========================================
# 🔗 DATABASE CONFIGURATION
# ==========================================

# El docker-compose.yml ya configura una base de datos PostgreSQL
# Aquí puedes la autenticación para conectarte a ella
N8N_DATABASE_USER=n8n_user
N8N_DATABASE_PASSWORD=your_database_password_here